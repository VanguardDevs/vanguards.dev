import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#fc4b08',
        },
        secondary: {
            main: '#fff',
            light: '#878787',
            dark: '#1e1e1e'
        }
    }
});

export default theme;
